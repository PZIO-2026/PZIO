name: pytest

on:
    push:
    pull_request:
        branches: [master]

jobs:
    test-backend:
        name: Run pytest on backend
        runs-on: ubuntu-latest
        steps:
            - name: Checkout repository
              uses: actions/checkout@v6

            - name: Set up Python
              uses: actions/setup-python@v6
              with:
                  python-version: "3.12"
                  cache: "pip"

            - name: Install dependencies
              run: |
                  python -m pip install --upgrade pip
                  python -m pip install -e backend

            - name: Run tests and publish summary
              run: |
                  set +e
                  set -o pipefail
                  pytest backend --cov=pzio --cov-report term-missing:skip-covered | tee pytest_output.txt
                  rc=$?
                  echo '## Pytest Output' >> $GITHUB_STEP_SUMMARY
                  echo '```' >> $GITHUB_STEP_SUMMARY
                  cat pytest_output.txt >> $GITHUB_STEP_SUMMARY
                  echo '```' >> $GITHUB_STEP_SUMMARY
                  exit $rc
